import 'package:flutter/material.dart';

class PaginaPerfilUsuari extends StatelessWidget {
  const PaginaPerfilUsuari({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Perfil d'usuari"),
    );
  }
}